# EffectTester
Simple Unturned Command to test effects in-game.

## How it works
  Use the command and an integer value to spawn an Unturned effect on the player.

## Commands
  ```/caineffect <int>``` - Spawns the effect with the matching integer ID at the player location <br>
  ```/e <int>``` - Alias of above command for quick typing
